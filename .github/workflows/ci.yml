name: CI

on:
  pull_request:
  push:
    branches: [ "**" ]

jobs:
  consistency:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Architecture consistency checklist
        run: ./ci/architecture-check.sh
